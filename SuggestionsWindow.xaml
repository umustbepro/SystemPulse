<Window x:Class="SystemPulse.SuggestionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="SystemPulse Suggestions" Icon="pack://application:,,,/Assets/SystemPulse.ico"
        Width="860" Height="740" MinWidth="560" MinHeight="460"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="CanResize"
        Background="{DynamicResource WindowBackground}" ShowInTaskbar="False">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <DropShadowEffect x:Key="SuggestionShadow" BlurRadius="16" ShadowDepth="3" Opacity="0.14" Color="#000000" />
        <Style x:Key="SuggestionCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="9" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Margin" Value="0,0,0,13" />
            <Setter Property="Effect" Value="{StaticResource SuggestionShadow}" />
        </Style>
        <Style x:Key="CandidateEndTaskButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{DynamicResource Danger}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="13,7" />
            <Setter Property="MinWidth" Value="82" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="52" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Border Background="{DynamicResource Surface}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" Margin="18,0" VerticalAlignment="Center">
                    <Image Width="26" Height="26" Source="pack://application:,,,/Assets/SystemPulse.ico" Stretch="Uniform" SnapsToDevicePixels="True" />
                    <TextBlock Text="SystemPulse" Margin="10,0,0,0" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <TextBlock Text="Suggestions" Margin="9,1,0,0" FontSize="12" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" />
                </StackPanel>
                <Button HorizontalAlignment="Right" Style="{StaticResource IconButton}" Width="46" Height="51" ToolTip="Close" Click="CloseButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True"><Viewbox Width="12" Height="12"><Canvas Width="16" Height="16"><Path Data="M 3,3 L 13,13 M 13,3 L 3,13" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" /></Canvas></Viewbox></Button>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="30,25,30,25">
                <TextBlock Text="{Binding SuggestionsTitle}" FontSize="27" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBlock Text="Fixes selected from the live performance and hardware conditions SystemPulse detected." Foreground="{DynamicResource TextSecondary}" FontSize="13" Margin="0,5,0,20" TextWrapping="Wrap" />

                <Border x:Name="CandidateSection" Style="{StaticResource SuggestionCard}" BorderBrush="#50FFB454">
                    <StackPanel>
                        <TextBlock Text="RESOURCE-HEAVY APPS YOU CAN CLOSE" Foreground="{DynamicResource Warning}" FontSize="11" FontWeight="Bold" TextWrapping="Wrap" />
                        <TextBlock Text="These are non-system background apps using resources related to this alert. End only apps you recognize and are not currently using; unsaved work can be lost." Foreground="{DynamicResource TextSecondary}" FontSize="12" Margin="0,7,0,12" TextWrapping="Wrap" />
                        <ItemsControl ItemsSource="{Binding CloseCandidates}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="0,11">
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                            <StackPanel Margin="0,0,14,0">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                <TextBlock Text="{Binding ResourceSummary}" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="End task" Style="{StaticResource CandidateEndTaskButton}" VerticalAlignment="Center" ToolTip="Immediately end this app and its child processes. Unsaved work can be lost." Click="EndTaskButton_Click" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock x:Name="EndTaskStatusText" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <ItemsControl ItemsSource="{Binding Suggestions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SuggestionCard}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <TextBlock Text="{Binding Explanation}" Foreground="{DynamicResource TextSecondary}" FontSize="12" Margin="0,7,0,10" TextWrapping="Wrap" />
                                    <ItemsControl ItemsSource="{Binding Steps}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,5,0,0">
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="18" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                                    <TextBlock Text="•" Foreground="{DynamicResource Accent}" FontWeight="Bold" />
                                                    <TextBlock Grid.Column="1" Text="{Binding}" Foreground="{DynamicResource TextSecondary}" FontSize="12" TextWrapping="Wrap" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Background="#0B58D6C7" BorderBrush="#3058D6C7" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,4,0,0">
                    <TextBlock Text="These suggestions are based on live correlations. Change one setting at a time so you can confirm which adjustment fixes the issue." Foreground="{DynamicResource TextSecondary}" FontSize="11" TextWrapping="Wrap" />
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
