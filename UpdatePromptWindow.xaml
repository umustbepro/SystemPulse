<Window x:Class="SystemPulse.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="SystemPulse update" Width="600" Height="390" MinWidth="440" MinHeight="330"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="CanResize"
        Background="{DynamicResource WindowBackground}" ShowInTaskbar="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="52" /><RowDefinition Height="*" /></Grid.RowDefinitions>

        <Border Background="{DynamicResource Surface}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" Margin="18,0" VerticalAlignment="Center">
                    <Border Width="26" Height="26" CornerRadius="6" Background="{DynamicResource Accent}"><Viewbox Width="17" Height="17"><Canvas Width="24" Height="24"><Path Data="M 3,12 C 6,7 8,17 11,12 C 14,7 16,17 21,12" Stroke="#071311" StrokeThickness="2.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Fill="Transparent" /></Canvas></Viewbox></Border>
                    <TextBlock Text="SystemPulse" Margin="10,0,0,0" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <TextBlock Text="Update" Margin="9,1,0,0" FontSize="12" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" />
                </StackPanel>
                <Button HorizontalAlignment="Right" Style="{StaticResource IconButton}" Width="46" Height="51" ToolTip="Skip this update" Click="SkipButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True"><Viewbox Width="12" Height="12"><Canvas Width="16" Height="16"><Path Data="M 3,3 L 13,13 M 13,3 L 3,13" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" /></Canvas></Viewbox></Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="30,26,30,28">
            <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Border Width="56" Height="56" CornerRadius="12" Background="#18FFB454" BorderBrush="#45FFB454" BorderThickness="1" VerticalAlignment="Top">
                    <TextBlock Text="&#xE895;" FontFamily="Segoe Fluent Icons" Foreground="{DynamicResource Warning}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Margin="18,0,0,0">
                    <TextBlock Text="A new SystemPulse version is ready" FontSize="21" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock x:Name="VersionText" Foreground="{DynamicResource Warning}" FontSize="13" FontWeight="SemiBold" Margin="0,7,0,0" TextWrapping="Wrap" />
                    <TextBlock Text="SystemPulse will download and install the update automatically. Your settings and saved monitoring data will be kept." Foreground="{DynamicResource TextSecondary}" FontSize="12" Margin="0,12,0,0" TextWrapping="Wrap" />
                    <Border Background="{DynamicResource SurfaceRaised}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14,11" Margin="0,17,0,0">
                        <TextBlock x:Name="CountdownText" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </Grid>

            <WrapPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Skip this update" Style="{StaticResource SecondaryButton}" Click="SkipButton_Click" Margin="0,0,10,8" />
                <Button Content="Start download now" Style="{StaticResource PrimaryButton}" Click="DownloadNowButton_Click" Margin="0,0,0,8" />
            </WrapPanel>
        </Grid>
    </Grid>
</Window>
